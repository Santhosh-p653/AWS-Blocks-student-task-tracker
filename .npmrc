# This tells pnpm to hoist packages to node_modules root.
# Required because AWS Blocks packages need to resolve each other
# in the same way npm does — without pnpm's strict isolation.
shamefully-hoist=true
